# Architecture

Every diagram below reflects the actual implemented code — the FSM
transition table, the fallback chains, the real endpoint shapes — not
an idealized version of the design. Where the diagrams and the code
could ever drift, the code wins; these are generated by hand against
`orchestrator.py`'s `VALID_TRANSITIONS`, each agent's real fallback
`try`/`except` chain, and the dashboard's real Zustand store, not
copied from planning documents.

## 1. System layers

```mermaid
flowchart TB
    subgraph Agents["Agent Layer"]
        Sentinel["SentinelAgent<br/>loop detection"]
        Triage["TriageAgent<br/>root-cause diagnosis"]
        Remediation["RemediationAgent<br/>patch + verify"]
        Optimization["OptimizationAgent<br/>workload reroute"]
    end

    subgraph FSM["Orchestration Layer"]
        Orchestrator["Orchestrator<br/>7-state FSM + TrustChain audit log"]
    end

    subgraph Bus["Event Bus Layer"]
        EventBus["EventBus<br/>asyncio.Queue pub/sub"]
    end

    subgraph Services["External Service Layer"]
        NIM["NVIDIA NIM<br/>embeddings"]
        Nemotron["Nemotron<br/>via NIM"]
        Groq["Groq<br/>Llama 3.3 70B"]
        ORTools["OR-Tools<br/>ILP solver"]
        NemoClaw["NemoClaw CLI<br/>sandbox exec"]
    end

    subgraph Infra["Infrastructure Layer"]
        Backend["FastAPI backend :8000"]
        Wrapper["Wrapper service :8081"]
        Dashboard["React dashboard :3000"]
    end

    Sentinel -->|LOOP_SUSPECTED| EventBus
    EventBus -->|dispatches concurrently| Orchestrator
    EventBus -->|dispatches concurrently| Optimization
    Orchestrator --> Triage
    Orchestrator --> Remediation
    Triage -.fallback.-> Nemotron
    Triage -.fallback.-> Groq
    Sentinel -.fallback.-> NIM
    Optimization -.fallback.-> ORTools
    Remediation -.calls.-> Wrapper
    Wrapper -.fallback.-> NemoClaw
    Backend --- Orchestrator
    Backend ---|WebSocket| Dashboard
```

## 2. Healing workflow — the FSM

`sentinel/agents/orchestrator.py`'s `VALID_TRANSITIONS` table, drawn
exactly (including the two paths most diagrams simplify away: a
worker can heal itself back to `HEALTHY` without ever looping, and
`RESUMED` can loop again later — `ESCALATED` is the only truly
terminal state, by design, since it means a human needs to look at it).

```mermaid
stateDiagram-v2
    [*] --> HEALTHY
    HEALTHY --> LOOP_SUSPECTED: SentinelAgent detects similarity > 0.92
    LOOP_SUSPECTED --> DIAGNOSING: Orchestrator dispatches Triage
    LOOP_SUSPECTED --> HEALTHY
    DIAGNOSING --> REMEDIATING: confidence >= 0.7
    DIAGNOSING --> ESCALATED: confidence < 0.7
    REMEDIATING --> VERIFYING: wrapper call attempted
    VERIFYING --> RESUMED: verified = true
    VERIFYING --> ESCALATED: verified = false
    RESUMED --> HEALTHY
    RESUMED --> LOOP_SUSPECTED: a new fault is injected later
    ESCALATED --> [*]: terminal — no automatic retry
```

## 3. End-to-end sequence — one healing cycle

```mermaid
sequenceDiagram
    participant U as BREAK IT button
    participant API as /demo/inject
    participant S as SentinelAgent
    participant Bus as EventBus
    participant O as Orchestrator
    participant T as TriageAgent
    participant OA as OptimizationAgent
    participant R as RemediationAgent
    participant W as Wrapper
    participant Audit as TrustChainLogger
    participant WS as Dashboard (WebSocket)

    U->>API: POST fault_type=schema_corruption
    API->>S: detect_loop() x4 (simulated repeats)
    S->>Bus: publish(LOOP_SUSPECTED)
    par concurrent dispatch
        Bus->>O: on_loop_suspected()
    and
        Bus->>OA: on_loop_suspected()
    end
    O->>Audit: log_transition HEALTHY to LOOP_SUSPECTED
    O->>WS: broadcast state_change + audit_event
    O->>T: diagnose(log_lines)
    O->>Audit: log_transition to DIAGNOSING
    OA->>Bus: publish(OPTIMIZATION_COMPLETE)
    Bus->>O: on_optimization_complete()
    O->>Audit: log_transition (side-channel, real solver result)
    O->>R: remediate(diagnosis)
    R->>W: POST /v1/remediate
    W-->>R: {verified, mode, flagged}
    O->>Audit: log_transition to VERIFYING then RESUMED
    O->>WS: broadcast state_change + audit_event
    WS->>WS: fetch /api/metrics on REMEDIATION_SUCCESS
```

## 4. Fallback chains

Every agent has its own independent fallback chain — this is the
core resilience pattern the whole project is built around. Each one
is a real `try`/`except` cascade in the code, not a conceptual diagram.

```mermaid
flowchart LR
    subgraph Sentinel["Sentinel — embeddings"]
        A1[NIM] -->|fails| A2[sentence-transformers]
        A2 -->|fails| A3[hash exact-match]
    end
    subgraph Triage["Triage — diagnosis"]
        B1[Nemotron] -->|fails| B2[Groq]
        B2 -->|fails| B3[rule-based heuristic]
    end
    subgraph Optimization["Optimization — reroute"]
        C1["cuOpt (skipped project-wide)"] -.never attempted.-> C2[OR-Tools ILP]
        C2 -->|fails| C3[greedy round-robin]
    end
    subgraph Remediation["Remediation — patch verify"]
        D1[NemoClaw CLI] -->|fails/times out| D2[mock wrapper]
        D2 -->|circuit open| D3[escalate to human]
    end
```

**Why cuOpt shows as "never attempted," not "failing":** hosted cuOpt
API access was never confirmed working during Phase 1 (no stable public
endpoint, `HTTP 000` on every attempted call) — this was a deliberate,
documented scope decision from Day 4-5, not a gap. OR-Tools was always
the intended fallback *for* cuOpt, so it's now the practical primary
solver. See `docs/api_contracts.md`'s "Optimization Agent — Solver
Status" section for the full history.

## 5. Dashboard data flow

```mermaid
flowchart LR
    WS["WebSocket /ws/stream<br/>(state_change, audit_event,<br/>stdout, stderr, mock_banner)"]
    Store["Zustand store<br/>(dashboardStore.ts)"]
    DAG["WorkflowDAG<br/>(React Flow)"]
    Health["HealthIndicators"]
    Audit["AuditLogStream"]
    Circuit["CircuitBreakerPanel<br/>(polls /api/circuit-status)"]
    Terminal["SandboxTerminal"]
    Triage["TriageReportCard"]
    Report["PostHealReportCard<br/>(fetches /api/metrics)"]

    WS -->|state_change| Store
    WS -->|audit_event: fallback_origin| Store
    Store --> DAG
    Store --> Health
    WS -->|audit_event| Audit
    WS -->|stdout/stderr/mock_banner| Terminal
    WS -->|audit_event: root_cause| Triage
    WS -->|state_change: REMEDIATION_SUCCESS| Report
```

## 6. Audit log hash chain

Every state transition is a hash-chained, append-only JSONL record —
tampering any line breaks verification for every record after it.

```mermaid
flowchart LR
    G["genesis<br/>hash = 0000...0000"] --> R1["record 1<br/>prev_hash = genesis<br/>current_hash = SHA256(prev + content)"]
    R1 --> R2["record 2<br/>prev_hash = record1.current_hash"]
    R2 --> R3["record 3<br/>prev_hash = record2.current_hash"]
    R3 -.->|verify_chain walks forward,<br/>recomputes every hash| Check{"any mismatch?"}
    Check -->|no| Valid["chain valid"]
    Check -->|yes| Broken["chain broken —<br/>see scripts/verify_audit_chain.py"]
```
